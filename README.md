# Images

This project uses high-quality remote image URLs (Unsplash + pravatar) referenced directly in `script.js` via the `IMG` map. No local binary assets are required — all product images load over HTTPS for fast CDN delivery.

To use local images instead, drop files in this folder and replace the URLs in `IMG` (top of `script.js`) with relative paths like `images/headset1.jpg`.
